import { CompletionProvider } from "./dist/amd/server/src/completionProvider";
import { Validator } from "./dist/amd/server/src/validator";

export const Server = {
    CompletionProvider,
    Validator
}
