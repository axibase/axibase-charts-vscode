import { CompletionProvider } from "./dist/server/src/completionProvider";
import { Validator } from "./dist/server/src/validator";

export class Server {
    public static CompletionProvider: CompletionProvider;
    public static Validator: Validator;
}
